<nav class="navbar navbar-expand-lg navbar-dark fixed-top">
    <a class="navbar-brand" href="/">
        <img src="{{ asset('build/images/logo.png') }}" class="d-inline-block align-top logo" alt="Wild Series"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('category_index') }}" aria-disabled="true">Catégories</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('program_index') }}" aria-disabled="true">Séries</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('season_index') }}" aria-disabled="true">Saisons</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('episode_index') }}" aria-disabled="true">Episodes</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('actor_index') }}" aria-disabled="true">Acteurs</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#" aria-disabled="true"><i class="fas fa-heart"></i> Favoris</a>
            </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
            <ul class="navbar-nav ml-auto align-middle">
                <li class="nav-item">
                    {% if app.user %}
                <li><a href="{{ path('app_logout') }}" class="nav-link">Déconnexion</a></li>
                <li><a href="{{ path('user_profile') }}" class="nav-link">{{ app.user.username }}</a></li>
                <li class="nav-item">
                    <img src="{{ app.user.picture }}" class="profil-pic rounded-circle">
                </li>
                {% else %}
                    <li>
                        <a href="{{ path('app_login') }}" class="nav-link">Connexion</a>
                    </li>
                {% endif %}
                </li>
            </ul>

        </form>
    </div>
</nav>