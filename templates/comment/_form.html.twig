<div class="container">
    {% form_theme form 'bootstrap_4_layout.html.twig' %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary"><i class="far fa-comment mr-2"></i> {{ button_label|default('Envoyer') }}</button>
    {{ form_end(form) }}
</div>