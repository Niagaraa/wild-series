{% extends 'base.html.twig' %}

{% block title %}Modifier un acteur{% endblock %}

{% block body %}
    <h1>Modifier un acteur</h1>

    {{ include('actor/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('actor_index') }}" class="btn">Retour à la liste des acteurs</a>

    {{ include('actor/_delete_form.html.twig') }}
{% endblock %}
