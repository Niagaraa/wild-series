{% extends 'base.html.twig' %}

{% block title %}Ajouter un genre{% endblock %}

{% block body %}

    <div class="container mt-4">
        <a href="{{ path('category_index') }}" class="btn btn-primary">
            <i class="fas fa-reply mr-2"></i> Retour à la liste des genres
        </a>

        <h1>Ajouter un nouveau genre</h1>
        <hr>

        {{ include('category/_form.html.twig') }}
    </div>
{% endblock %}
