{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">
        <i class="fas fa-save mr-2"></i> {{ button_label|default('Enregistrer') }}
    </button>
{{ form_end(form) }}
